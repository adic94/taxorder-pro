# Changelog — TaxOrder Pro

Wszystkie istotne zmiany w projekcie. Format zgodny z [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased] — 2026-07-09
### 🔒 Bezpieczeństwo
- Security: XSS hardening + bug fixes + nowe moduły (error-tracker, DT-1 history, eToll, webhooks UI) (`17d3a4f1`)
- Security: S-03 CSP headers, S-04 SRI hashes, S-05 session company isolation (`79910e56`)
- Security: XSS — escapeHtml() we wszystkich innerHTML interpolacjach danych użytkownika (`0bcdb3f9`)

### 🐛 Poprawki
- Fix: 3 krytyczne bugi + GPS KPI karta w pasku pojazdów (`df1aac4c`)
- Fix: URL aktualizacja przy otwarciu/zamknieciu karty pojazdu; nowe strony w ROLE_TABS (`bc054253`)
- Fix: vehicleAlerts -> allAlerts w queueNotificationJobs (3 miejsca) (`85d70194`)
- Fix: CSP worker-src rozszerzony o cdn.jsdelivr.net i unpkg.com (`f7dfdd36`)
- Fix: SW cache v6 — fleet-cloud.js nie istnieje → PWA instalacja failowała (`914bf8f0`)
- Fix: CF_WORKER_URL alias w cf-config.js — naprawia CEPiK i direct API calls (`1e1ba563`)
- Fix: tnb-kierowcy i tnb-kalendarz — dodaj id + wpisy do ROLE_TABS (`1227b86b`)
- Fix: dodaj brakujący include dt1-generator.js w index.html (`3bb82918`)
- Fix: poprawne parsowanie nowego formatu DR w aztec-scanner.js + podgląd DR w karcie pojazdu (`a529179d`)
- Fix: KPI fines chip shows all unpaid fines + remove dead storage.js methods (`018cd51e`)
- Fix: fines cache corruption + paginacja driver/reservation + walidacja dat (`3e5a4ad1`)
- Fix: eksport/import — dodano rezerwacje do EXPORT_TABLES, backup info (`75f3bba4`)
- Fix: karty flotowe w karcie pojazdu — migracja ze starego localStorage do D1 (`0d5c3dab`)
- Fix: powiadomienia — pole licenseExpiry → license_expiry (snake_case D1) (`e5e36260`)
- Fix: zakładka Mandaty w karcie pojazdu — async renderForVehicle (`b4af2e62`)
- Fix: raporty kierowcow — getAllSync() zamiast async getAll(), snake_case pol mandatow (`17a89c3a`)
- Fix: _isAdmin() i _currentUser() w fleet-calendar.js — window.currentUser zamiast localStorage (`cc59c561`)
- Fix: async/sync mismatch w kartach mandatow na dashboardzie (`1a594c66`)
- Fix: 2 bugi — eksport/import fines i drivers, alerty push driver_license + fine_deadline (`5d963e0c`)
- Fix: 6 bugów powiadomień — NS role_tabs, parsowanie API responses, 401/403, data.applied, company param (`660f5e03`)
- Fix: 5 bledow Faza 1 — alert-types DELETE guard, notif-log filter, prefs encoding, templates apply, user permissions field (`6424ae10`)
- Fix: testy QA — paginacja + nowy test POST /api/import round-trip (`1e81b17f`)
- Fix: 4 błędy logiczne — avg l/100km, duplicate badge IDs, hs-token-store, vSel init (`4ff84eea`)
- Fix: 4 bledy UX — Karty flotowe w dedykowanej zakladce, licznik eksportu dynamiczny, hint podatnika, http-server/playwright w devDeps (`1ff9f985`)
- Fix: tab bar karty pojazdu — strzalki nawigacji zamiast obcietego tekstu (`b9e18057`)
- Fix: sw.js — nowe moduły w cache PWA + bump do taxorder-v5 (`fa9697b6`)
- Fix: dual-name field fallback (dmc/dmcMax/dmcKg, dataRejestracji/dataRej itp.) + nowe moduly (`e38d57da`)
- Fix: OCR sanityzacja + CEPIK integracja w formularzu OCR + WOJ_MAP 3-literowe prefiksy (`a19bf3d2`)
- Fix: OCR - normalizacja nrRej (spacje), najwczesniejsza dataRej, walidacja G<F.1 (`94f2a8c4`)
- Fix: OCR - wybierz MAX wartość F.1/F.2/F.3 zamiast pierwszej znalezionej (`5753851a`)
- Fix: usun pre-download modeli z build (SIGABRT OOM w builderze) (`81aec9ed`)
- Fix: add build-essential (gcc) for stringzilla compilation (`06a7e9ae`)
- Fix: paddlepaddle 2.6.2 + paddleocr<3.0.0 (2.6.1 nie istnieje) (`9961aa85`)
- Fix: libgl1-mesa-glx → libgl1 (Debian Trixie) (`818cd43c`)
- Fix: AI Vision model fallback, auto AI-text po OCR, czyszczenie stanu przy nowym dokumencie (`5dc88057`)
- Fix: OCR – lepsza walidacja nr rej, przyciski AI Vision zawsze widoczne, fallback paliwo/data (`977ec96c`)
- Fix: OCR — usunieto podwojne enhancowanie obrazu PDF (`a3dc985d`)
- Fix: OCR — pełna obsługa 4 rotacji (0/90/180/270°) + poprawka osi L (`1402af14`)
- Fix: OCR PDF — AI fallback + subscribeRealTime + PDF.js v-match (`bc6d534d`)
- Fix: CF_WORKER_URL konfigurowalne + auto server-push przy pilnych alertach (`f0ba3161`)
- Fix: Dark mode icon — ustaw ikonę po DOMContentLoaded (`d06dbf99`)
- Fix: Dark mode — toast, inputs, formularz DT-1 (zachowany biały) (`a3baad76`)
- Fix: Mobile topbar — zachowaj pole Rok na telefonach (kompaktowe) (`e0bcc529`)
- Fix: DT-1 XML — CelZlozenia z selecta + OsobaNiefizyczna dla firm (`d668b356`)
- Fix: i18n — html lang attr, smart toast auto-translate, vehicle-detail modal tabs/buttons (`d5853b9e`)
- Fix: bugfixes + ulepszenia UX we wszystkich nowych modułach (`4c25e53b`)
- Fix: model AI llama-3.1-8b → llama-3.3-70b-versatile (lepsza jakość) (`8665a87e`)
- Fix: AI → Groq API (darmowy Llama 3.1 8B) zamiast CF Workers AI (`e982b1a9`)
- Fix: AI CORS + try-catch + zmiana modelu na mistral-7b-instruct-v0.2 (`64a0ea7c`)
- Fix: AI → Cloudflare Workers AI (darmowy) zamiast Anthropic API (`10b07874`)
- Fix: window.vehs nie było na window — pojazdy z D1 nigdy nie trafiały do stanu app (`e0dad64f`)
- Fix: window.selected/currentCompanyId nie były na window; błędy Supabase w logach; gitignore (`f953b761`)
- Fix: rok DT-1 y=667 - powyzej dolnej ramki kratki (y=664.87) (`30c5df74`)
- Fix: usun --verbose (wrangler 4 nie obsluguje tej flagi) (`34519a62`)
- Fix: workflow z pelnym logiem wranglera zamiast wrangler-action (`40b17ae8`)
- Fix: zmiana nazwy workera na taxorder-pro-api + package.json (`332598ec`)
- Fix: dodaj account_id do wrangler.toml (`6988cfcf`)
- Fix: zwolnienie pojazdów specjalnych z podatku DT-1 (`a2f46358`)
- Fix: radio checkboxy DT-1/A + nowe pola pojazdu (DT-1/A poz. 8-12) (`a1b2b38c`)
- Fix: uzyj oryginalnych PDF z polami AcroForm (720KB) zamiast plskich bez pol (`3f705c65`)
- Fix: uzywaj oficjalnych formularzy DT-1(6)/DT-1/A(6) jako szablonu PDF (`8edaa6f1`)
- Fix: DT1Generator z danymi z bazy, transliteracja PL (`9d63c250`)
- Fix: aktywny PDF (bez flatten), pola Powiat i Gmina konfigurowalne (`c5fd9556`)
- Fix: wylacz generator od zera, uzyj oficjalnych PDF MF jako szablonow (`542f0a0f`)
- Fix: usun zduplikowana funkcje _pl2ascii i wlasciwosc _useAsciiOnly (`e0ecbe81`)
- Fix: transliteracja PL w dt1-generator (`a7379a6b`)
- Fix: transliteracja PL w dt1-generator - PDF bez bledow WinAnsi (`caa8e223`)
- Fix: rok DT-1(6) x=218 y=657 w fallback generatorze (`3c16e39a`)
- Fix: dt1-generator zgodny z DT-1(6) i DT-1/A(6) - aktualna wersja formularza (`38ab6a75`)
- Fix: transliteracja polskich znakow w dt1-generator (`6d4a9bc4`)
- Fix: DT1Generator w PDF + kompaktowy topbar (`00e87f1e`)
- Fix: DT1Generator wywolywany w pobierzWypelnionyPDF, topbar kompaktowy (`632f9968`)
- Fix: rok PDF z taxYearDT1 zamiast globalnego taxYear (`93d50767`)
- Fix: rok DT-1 z taxYearDT1, usunieto podwojne spacje w drawText (`3dd58f20`)
- Fix: poprawiono pozycje roku w PDF DT-1 (`95c0a862`)
- Fix: pozycja roku DT-1 oraz wybor okresu deklaracji (`58eb8887`)
- Fix: reset hasla - modal HTML zamiast prompt, obsluga code i hash flow (`075c0563`)
- fix: remove duplicate let rawText180 — SyntaxError causing login failure (`b3a948f2`)
- fix: sync company data, filtry wlasciciel, poz 7/18/19, CEPiK fields, OCR MRZ (`39fe8296`)
- Fix _redirects - use standard 200 status for SPA routing (Cloudflare Pages native) (`90a08638`)
- Fix _redirects infinite loop - add 200! status to bypass cache/redirects for SPA routing (`9c09fe91`)

### ✨ Nowe funkcje
- Add: CLAUDE.md (reguły projektu dla AI) + xss-audit.js (autotest XSS) (`eb694fe5`)
- Add: GPS dot w liście pojazdów, alerty km serwisowe, auto-refresh mapy (`689b921a`)
- Add: Mapa GPS Floty — strona z pozycjami GPS wszystkich pojazdów na Leaflet (`739911ce`)
- Add: anomalie spalania, eksport CSV paliwa, cloud backup/restore, TCO widget, fix top10 l/100km (`df92d189`)
- Add: e-Toll balance check via Tekom API + UI widget (`66560371`)
- Add: Tekom MyCar API integration — auto-sync km i GPS z systemu GPS (`57fd6d11`)
- Add: kolorowanie wierszy tabeli wg alertow terminow, ikona uwag pojazdu (`de03e957`)
- Add: klikalne wiersze dash-alerts, widget Ostatnia aktywnosc floty (`cbe0ce70`)
- Add: rozszerzone filtry pojazdow + quickFilterVeh() z KPI cards (`a137ec8d`)
- Add: KPI Dashboard server-side stats, klikalny strip, podglad alertow w powiadomieniach (`443c5ba2`)
- Add: Alert Dashboard, deep link pojazdu, _copyLink, przycisk UFG (`373d4467`)
- Add: Import DR z kodem Aztec z folderu R2 (`c6252991`)
- Add: email per-alert, OCR polis z R2, zmiana nr rej z archiwizacja (`26b41f2e`)
- Add: i18n dla fleet-calendar.js i notifications.js (7 języków) (`783d3351`)
- Add: Backup v3 — eksport/import D1 (mandaty, kierowcy, karty, rezerwacje) przez /api/export (`1f198fac`)
- Add: Kalendarz floty D1 — handleReservations, migracja fleet-calendar.js z localStorage, schema_v13, detekcja konfliktow (`8eb95710`)
- Add: Karty flotowe D1 — handleFleetCards, migracja z localStorage, schema_v12 (`2b2d83e4`)
- Add: widget prawa jazdy kierowcow na dashboardzie + licznik kierowcow w KPI (`54c7d8a6`)
- Add: getUnpaidAlertsSync() i load() w FinesModule — sync wrapper dla dashboardu (`12fec27a`)
- Add: Alerty prawo jazdy i mandaty w systemie powiadomień push (`08acfc19`)
- Add: Kierowcy D1 — handleDrivers, migracja drivers.js z localStorage, schema_v11 (`b238c967`)
- Add: Moduł Mandatów D1 — handleFines, migracja fines.js z localStorage, schema_v10, ROLE_TABS (`47b068c7`)
- Add: /api/notif-trigger + przycisk 'Wyzwol kolejke' w centrum powiadomien (`55a198f2`)
- Add: Cloudflare Queues dla powiadomien — async push/email/sms z retryami (`9abded35`)
- Add: system powiadomieniu Faza 1 — alert_types, prefs, log, templates, konserwacja (`6ed4526e`)
- Add: powiadomienia push — schema_v7 + rejestracja SW + nowe klucze VAPID (`b909f954`)
- Add: zestaw narzędzi QA — Human Sim, Diagnostics, Playwright runner (`fcb1f6a8`)
- Add: usuwanie pojazdu z karty pojazdu (`c1cc9396`)
- Add: cron push alert — Worker wysyła powiadomienia push o wygasających terminach (`af2ba487`)
- Add: CEPiK sync — bezposrednie wywolanie Worker proxy z parsowaniem JSON:API (`44b2f6cf`)
- Add: OCR - pola O.1, O.2 (masa przyczepy) i K (nr homologacji) z dowodu rej. (`4dfdd9ac`)
- Add: Python PaddleOCR service + integracja z Worker jako primary OCR (`920b9060`)
- Add: odczyt kodu AZTEC 2D z polskich DR — NRV2E decoder + ZXing browser + endpoint /api/aztec (`5e4e7683`)
- Add: AI Vision OCR (Groq llama-4-scout) + fix extractOcrWithAI (`cb7c756b`)
- Add: wyszukiwanie per kolumna, kolumny DR, AC/NNW checkbox, karty flotowe modal, historia UDT/Tacho/SKP z NIP, ślad audytu, wyszukiwanie w historii serwisu, NIP nabywcy (`28f3b087`)
- Add: Serwis NIP+waluta+VAT, opony DOT+blizniaki, dwuklik=karta, OCR DR (`9b221feb`)
- Add: Ewidencja floty — widok flotowy, karty pojazdow, KPI strip (`490177dc`)
- Add: VAPID Web Push — pełna implementacja serwer + klient (`a737fa72`)
- Add: i18n (PL/EN/DE/UA) dla modali backup, import pojazdów i stawki gmin (`c5789823`)
- Add: mapa GPS Leaflet w zakładce GPS karty pojazdu (`d4667f7a`)
- Add: Ciemny motyw (dark mode) + kompaktowy topbar na mobile (`1250a014`)
- Add: Raport zarządu — przycisk Wyślij e-mail (mailto) (`06799297`)
- Add: SW push handler + notificationclick + rozszerzona lista cache (`029db6b2`)
- Add: Centrum Powiadomień — bell topbar + aktywne alerty floty (`b4a2c913`)
- Add: Supabase real-time sync + responsywność mobilna (`ab53a8fc`)
- Add: i18n (PL/EN/DE/UA), gminy rates, Excel import, backup, GPS tab, CEPiK proxy sync, i18n coverage (`86274031`)
- Add: TEKOM GPS import, DT-1 XML export, role Kierowca/Dyspozytor, statystyki kierowców (`17458924`)
- Add: mandaty, dokumenty, PWA, l/100km, kierowca stats, KOBIZE reminder (`3f8dc840`)
- Add: Moduł Serwisowy + Raporty Flotowe + Opony (priorytety A+B+C) (`f9fdacd1`)
- Add: CEPiK XML import + KOBIZE CO2 emissions calculator (`ecb7d58b`)
- Add files via upload (`7e7ff50f`)
- Add _redirects for Cloudflare Pages SPA routing (`904c0d72`)
- Add comprehensive README.md with Cloudflare Pages deployment instructions (`39feecb2`)
- Add .gitignore for Cloudflare Pages deployment (`bf1d682d`)
- Add files via upload (`e149e094`)

### 🔄 Aktualizacje
- Update: rozszerzone typy serwisów z zabudową pojazdu (`65a6e4ac`)
- Update: oficjalny formularz DT-1/A(6) do assets (`2dd2fa39`)
- Update: oficjalne formularze DT-1(6) i DT-1/A(6) z podatki.gov.pl (`eabc7526`)
- Update app.js (`50ac7d06`)
- Update app.js (`98665849`)
- Update app.js (`32d867fe`)
- Update app.js (`581a4c93`)
- Update style.css (`466b6a82`)
- Update app.js (`5314234e`)
- Update .gitignore - exclude wrangler.jsonc which conflicts with Cloudflare Pages (`ea4207b2`)

### ♻️ Refaktoryzacja
- Refactor: PaddleOCR → pytesseract (OOM fix) + spatial section filter + max-F1 selector (`df9af6e9`)

### 📝 Dokumentacja
- docs: update README with current state (`442f505f`)

### 🔧 Narzędzia
- chore: update fontkit.umd.min.js (`13eaf387`)
- chore: update pdf-lib.min.js (`89bda46a`)

### 📦 Inne
- Add/Fix: serwisy km, GPS dashboard, poprawki pól danych, wyszukiwanie kierowcy (`03ec1343`)
- Fix+Add: eksport CSV filtrowanych pojazdow; eksport CSV alertow z dashboardu (`cf2cf0bb`)
- Feat: TaxOrderDrivers.importBulk() — masowy import kierowców z ZSIA (`6c024934`)
- Feat: dashboard przebudowany na karty + integracja ZSIA + fix topbar i18n (`aa554833`)
- Feat: import DR z PDF + DR Wariant B (side-by-side ze skanem) (`91f4c4f2`)
- Feat: GZIP/zlib fallback w dekoderze Aztec DR dla starszych dokumentów (`58f580c3`)
- ocr-service: NRV2E przez ctypes/libucl + AztecDecoder adapter + NOTICE (`b8f17118`)
- ocr-service: testy pytest (validators/nrv2e/preprocessing/parser) + CI z pytest (`497b6d61`)
- ocr-service: kompletny pipeline ekstrakcji DR (Aztec → OCR → Vision) (`2f7caad8`)
- ocr-service: zależności — dodaj pdf2image, zxing-cpp, anthropic + poppler-utils (`19ff2284`)
- Zabezpieczenia: przypięcie obrazu Docker + CI dla składni JS i buildu OCR (`b9faf737`)
- i18n: kompletne t() dla dmg/tw/cfminv/hp/fi/csvi/tki — 7 modułów, 287 nowych kluczy w i18n.js (7 języków) (`ac8cff30`)
- i18n: reports.js — 15 remaining hardcoded PL strings in 7 languages (`ea380a4f`)
- i18n: service-orders.js + reports.js — 18 hardcoded PL strings in 7 languages (`eded9fe5`)
- i18n: api-keys.js + notification-settings.js — 21 hardcoded PL strings in 7 languages (`31a24b77`)
- i18n: fines.js — 13 hardcoded PL toast calls → t() in 7 languages (`d333477a`)
- i18n: vehicle-detail.js — 22 hardcoded PL strings → t() in 7 languages (`e383f81c`)
- Feat: raport miesięczny PDF — jspdf-autotable, karta UI, generateMonthlyPdf() (`0dfc3b07`)
- Feat: paginacja pojazdów, webhoki GPS/paliwo, klucze API, testy QA (fazy 1-5) (`97701bba`)
- Feat: fleet-first redesign — nowy nawigator, dashboard i moduł Paliwa (`356b6957`)
- ﻿Fix: 5x raw JS template literals w index.html renderowaly sie jako tekst (`e6c02160`)
- ﻿Fix: _tab() null check prevents crash on unknown tab name (`aeaf30e2`)
- ﻿Add: tools/qa-tester - narzedzie QA do testowania DT-1, importu, OCR i API (`a7e5f9c3`)
- ﻿Add: printCard DT-1+TCO sekcje, import ostrzezenie o nierozp. kolumnach, raport ubezp. pojazdy bez OC (`25bd319b`)
- ﻿Add: walidacja OC/AC/przeglad + filtr wygaslych terminow (`71081925`)
- ﻿Add: DT-1 w karcie pojazdu + widget wieku floty na dashboardzie (`48e493aa`)
- Config: VAPID_PUBLIC_KEY dla Web Push (`531e4f64`)
- Feature: Kalendarz Floty — rezerwacje pojazdów (Gantt + miesiąc) (`bf6f6d44`)
- Feature: kartoteka kierowców z autocomplete w pojazdach (`5a9dc310`)
- Feature: moduł kosztów — tankowania z kart paliwowych (`0609a297`)
- Feature: pełny system CFM — karta pojazdu z DR, UDT, tachografem, polisami (`4c757097`)
- Feature: Asystent AI (Claude claude-haiku-4-5) w TaxOrder Pro (`74a13835`)
- UI: sidebar boczny zamiast poziomego paska nawigacji (`65c73ec0`)
- Cleanup: usuń martwy blok DT1Generator z pobierzWypelnionyPDF (`5b4c47e9`)
- Config: URL Cloudflare Worker taxorder-pro-api.adamus1000.workers.dev (`671d825b`)
- Trigger: wrangler 4 + nowy token (`a360d6e5`)
- Integracja Cloudflare: Worker + D1 + R2 + KV (`3bd31647`)
- Feature: dekoder kodu AZTEC z dowodu rejestracyjnego (`97ce762b`)
- Feat: dt1-generator v2 - oficjalne formularze DT-1(6) i DT-1/A(6) jako tlo (`363c560c`)
- Feat: DT1Generator wywolywany w pobierzWypelnionyPDF (`bcf6bf59`)
- Feat: podlaczono DT1Generator - wlasny PDF od zera, rok bez bledow (`8329729f`)
- Feat: wlasny generator PDF DT-1 niezalezny od formularza MF (`8e6d234f`)
- Feat: rok deklaracji edytowalny - wpisz dowolny rok (korekty za poprzednie lata) (`fdd51902`)
- Feat: podlaczenie modulow do index.html + modal karty pojazdu (`8dec7cde`)
- Feat: stawki z bazy, OCR PDF uchwal, karta pojazdu, import/eksport, launcher modulow, poprawka roku DT-1 (`f7009ad7`)
- Feat: karta pojazdu, import/eksport xls/csv, launcher modułów, leasing, archiwum (`849df119`)
- Feat: zapis stanu pojazdow do Supabase (osie, zawieszenie, DMC, miesiace) (`5fcca3ec`)
- Naprawiono obsluge kodu recovery Supabase (`0bf95e30`)
- Dodano obsluge linku resetu hasla (`b8daab19`)
- Dodano przycisk resetu hasla (`476b1654`)
- Poprawiono literowke async w doLogin (`22d2fd85`)
- Revert "Naprawiono blad async przy logowaniu" (`e2cbfc29`)
- Naprawiono blad async przy logowaniu (`ed137bd9`)
- Revert "Dodano przycisk resetu hasla w logowaniu" (`4f07aa78`)
- Dodano przycisk resetu hasla w logowaniu (`6678a345`)
- Dodano reset hasla przez email (`983437ec`)
- Poprawiono auth Supabase i reset hasla (`bced4ffd`)
- Podpieto company-access.js (`7e030456`)
- Dodano panel uprawnien firm dla uzytkownikow (`574a0176`)
- Dodano przycisk usuwania firmy (`80eac4e1`)
- Poprawiono przycisk dodawania firmy (`daf7f91d`)
- Poprawiono widocznosc modala dodawania firmy (`e3f807ca`)
- Dodano formularz recznego dodawania firm (`c6035b18`)
- Podpieto automatyczne renderowanie firm (`e4c3e5d7`)
- Revert "Podpieto wyswietlanie firm z Supabase w zakladce Firmy" (`d19f6b87`)
- Podpieto wyswietlanie firm z Supabase w zakladce Firmy (`c819196d`)
- Dodano wyswietlanie firm z Supabase (`8b9ec3f0`)
- Dodano bezpieczny odczyt firm z Supabase (`8970528d`)
- Revert Dodano panel zarzadzania firmami (`ed4cc924`)
- Dodano panel zarzadzania firmami (`3f83a61a`)
- Dodano modul firm z Supabase (`7e31eda0`)
- Odswiezono pulpit po zmianie spolki (`0bc59f52`)
- Zmieniono domyslnego uzytkownika logowania (`4dcc5ccd`)
- Naprawiono logowanie Supabase Auth przed ladowaniem floty (`7afac843`)
- Revert "Wypełniono rok DT-1 przez aktywne pole PDF" (`a7cb9f06`)
- Wypełniono rok DT-1 przez aktywne pole PDF (`fd10e7d9`)
- Wyłączono automatyczne łączenie CEPiK przy starcie (`2741d7fc`)
- Zabezpieczono flotę przed pustym odczytem z Supabase (`556d06ba`)
- Poprawiono pozycję roku DT-1 (`0b2d6b88`)
- Dopasowano pozycję roku w DT-1 (`11cdf9d7`)
- Dodano wpis roku do pola 4 DT-1 (`52de03a2`)
- Poprawiono numerację załączników DT-1A (`89c11e1d`)
- Poprawiono rok deklaracji DT-1 (`192ef85f`)
- Poprawiono pola rok, imię i poprzedni urząd DT-1 (`d00989c9`)
- Poprawiono rok deklaracji i pole 19 DT-1 (`0b8c2c18`)
- Poprawiono zaznaczanie pól wyboru DT-1 (`c88fbb2b`)
- Revert "Dodano zapis edycji pojazdu do Supabase" (`a0ecaae5`)
- Dodano zapis edycji pojazdu do Supabase (`7845ed7b`)
- Poprawiono konfiguracje MCP (`662dc4c4`)
- Dodano MCP Supabase do VS Code (`7a8bf6f2`)
- Dodano automatyczne ladowanie pojazdow z Supabase (`114c633c`)
- Dodano odczyt pojazdow z Supabase (`6ffb72de`)
- Dodano wyliczanie kategorii i podatku przy migracji (`c0677b60`)
- Poprawiono zrodlo pojazdow dla migracji Supabase (`dbe9ce0a`)
- Udostepniono pojazdy dla migracji Supabase (`7b0fd6f5`)
- Dodano migracje pojazdow do Supabase (`9ad152e6`)
- Dodano logowanie Supabase Auth (`5705acd6`)
- Naprawiono inicjalizacje klienta Supabase (`877f6adb`)
- Dodano klienta Supabase (`a02be1a6`)
- Dodano konfiguracje Supabase (`a05572a8`)
- Dodano konfiguracje Chrome DevTools MCP (`9228d388`)
- Dodano zasoby PDF i czcionke do Cloudflare (`e19c062b`)
- Przywrocono moduly storage i tax engine w index (`db2514f0`)
- Zapisano aktualny storage adapter (`9ec8ced9`)
- Usunieto fleet-manager-bundle z index (`1a3cc6a7`)
- Usunieto nieistniejacy fleet-manager.css z index (`3e40d85c`)
- Dodano moduł tax engine dla DT-1 (`1940744b`)
- Rozbudowano storage adapter pod pojazdy i integracje (`9fd6e2b9`)
- Dodano pierwszy moduł storage adapter (`757672e4`)
- Dodano mapowanie migracji localStorage do Supabase (`5ba86266`)
- Dodano podstawowy schemat Supabase (`2d6aba94`)
- Dodano architecture.md (`4a283ccf`)
- Uzupełniono vision i roadmap projektu (`7cf1bc4f`)
- Dodano dokumentację i strukturę TaxOrder Fleet Manager (`e4dd39c1`)
- feat: update style.css from user upload (`39ff46f1`)
- feat: update app.js from user upload (`9da12608`)
- feat: update index.html from user upload (`4cd07780`)
- feat: add login screen HTML, fix login system (login-screen div was missing) (`8a27acda`)
- feat: embed PDFs as base64 — no fetch() needed, works from file:// (`b6c4d7ca`)
- style: no changes (`35bf289e`)
- Delete _redirects (`2cf911b6`)
