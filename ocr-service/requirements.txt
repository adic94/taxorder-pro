fastapi==0.111.0
uvicorn[standard]==0.30.1
pytesseract>=0.3.10
Pillow>=10.0.0
numpy>=1.24.0,<2.0.0
opencv-python-headless>=4.8.0
python-multipart>=0.0.9
pdf2image>=1.17.0
zxing-cpp>=2.2.0
anthropic>=0.28.0
# PaddleOCR — silnik ekstrakcji od 25.08 (patrz extractors/paddle_fields.py).
# Zwykłe PyPI, bez niestandardowego indeksu — zweryfikowane (25.08), oba pakiety
# mają koła manylinux dla cp311/linux_x86_64, zgodne z bazą Dockerfile.
#
# paddlepaddle 3.3.1 — zmierzone na żywo (25.08), DWIE wersje padały na CPU
# akceleratorze oneDNN, każda inaczej: 3.3.1 rzucał "NotImplementedError:
# ConvertPirAttribute2RuntimeAttribute not support" (regresja PIR→oneDNN,
# PaddlePaddle/Paddle#77340) na KAŻDYM wywołaniu; cofnięcie do 3.2.2 ominęło TEN
# błąd, ale zamiast niego dało SIGFPE (twardy crash procesu) wewnątrz konwolucji
# oneDNN. Dwie różne awarie w tym samym podsystemie na dwóch wersjach — poprawka
# nie jest w wyborze wersji, tylko w wyłączeniu oneDNN w ogóle: patrz
# `enable_mkldnn=False` w extractors/paddle_fields.py. Zostaje aktualne wydanie.
paddlepaddle==3.3.1
paddleocr==3.7.0
# opcjonalnie: nrbf>=0.1.0  # parsowanie NRBF w GZIP-fallback dla starszych DR
